# Azure AD Configuration
AZURE_CLIENT_ID=client_id
AZURE_TENANT_ID=tenat_id

# Database Configuration  
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=ecommerce_db

# Security Configuration
NODE_ENV=development
PORT=3001
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Session Secret (generate a strong random string)
SESSION_SECRET=your-very-secure-random-string-here

# Admin Configuration
# Comma-separated list of admin emails
ADMIN_EMAILS=admin@yourcompany.com,superuser@yourcompany.com
# Comma-separated list of admin domains (users from these domains get admin access)
ADMIN_DOMAINS=yourcompany.com,admin.yourcompany.com